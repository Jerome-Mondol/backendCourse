<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <title>Document</title>
</head>
<body>
    <div class="w-full h-full bg-zinc-900 p-10 text-white" >
        <div class="nav mb-10" >
            <a class="text-blue-500 text-sm" href="/read">Read Users</a>
        </div>
        <h1 class="text-3xl" >Update User</h1>
        <hr class="mt-3" >
        <form action="/update/<%= user._id %>" method="post" class="flex flex-col w-[30%] gap-5 mt-5">
            <input class="px-5 py-3 bg-tranparent rounded-lg border-2 border-zinc-600 outline-none" type="text" placeholder="Enter your name" name="name" value="<%= user.name %>">
            <input class="px-5 py-3 bg-tranparent rounded-lg border-2 border-zinc-600 outline-none" type="text" placeholder="Enter your email" name="email" value="<%= user.email %>">
            <input class="px-5 py-3 bg-tranparent rounded-lg border-2 border-zinc-600 outline-none" type="text" placeholder="image url" name="image" value="<%= user.image %>">

            <input class="px-5 py-2 bg-orange-500 rounded-lg w-[50%] cursor-pointer" type="submit" value="Update User">
        </form>
    </div>
</body>
</html>
