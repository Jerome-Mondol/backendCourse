<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <title>Simple practice</title>
</head>
<body>
    <div class="main bg-zinc-900 h-screen">
        <div class="form w-full">
            <form action="/create" method="post" class="p-10 text-white">


                <input class="bg-zinc-700 rounded-md w-full px-5 py-4 outline-none" type="text" placeholder="Enter your name" name="name">
                <textarea class="bg-zinc-700 rounded-md w-full px-5 py-4 outline-none resize-none mt-3" placeholder="Tell us more about your self" name="details"></textarea>

                <input type="submit" class="px-5 py-3 bg-orange-500 rounded-md mt-3 cursor-pointer hover:bg-orange-600 transition duration-200" value="Submit">
            </form>
        </div>
        <div class="tasks p-10 text-white flex flex-wrap gap-5">
            <% if(files.length > 0) { %>
                <% files.forEach((val) => { %>
                    <div class="task w-64 bg-zinc-700 pl-2 rounded-md mt-3">
                        <h1 class="text-2xl mt-2" ><%= val %></h1>
                        <a class="mt-3 inline-block py-2 text-blue-500" href="">Read more about name</a>
                    </div>
                <% }) %>
            <% } %>
        </div>
    </div>
</body>
</html>
